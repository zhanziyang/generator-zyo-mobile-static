@require './global/*'

body
  position: relative

.box
  width: ps2rem(375px)
  background: blue
  absCenter()
  avatar('http://stylus-lang.com/img/stylus-logo.svg')
  box-shadow: psShadow(black, 75%, 90, 0px, 10, 40px), psShadow(grey, 65%, 60, 2px, 10, 170px, true)
